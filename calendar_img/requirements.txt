# calendar_todo plugin — Python dependencies
# Install with:
#   source /usr/local/inkypi/venv_inkypi/bin/activate
#   pip install -r requirements.txt

icalendar>=5.0.0
recurring-ical-events>=2.1.0
requests>=2.28.0
Pillow>=9.0.0
